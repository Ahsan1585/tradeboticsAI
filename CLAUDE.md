# CLAUDE.md

## Model selection

Before delegating any non-trivial task to a subagent (or choosing a model override), consult the `model-router` subagent first to get a cheapest-sufficient-model recommendation. It's advisory only — it returns `recommended_model` / `reasoning` / `requires_user_confirmation`. If it recommends Opus or Fable, ask the user to confirm before dispatching the real work at that tier. Routine Haiku/Sonnet recommendations proceed without asking.


lets use this information obtained and conduct a brainsotrming session to create a most robust fucntional tradebotics app that gives users an extreme edge when investing it should be very easy and user friendly to udnerstand that even the non stock person can get the idea of what stocks they can confidentally invest in. utilize the additional data obtained from the urls along with the existing fucntionality of tradebotics to build a most user friendly and robust application 

going forward dont prompt me to ask for things that dont require my absolute attention i am not a developer so only come to me ask for descision making questions that impact the design and the fundamentals of this app use the cheapest way to exewcute the tasks at hand so i can get more hours from claude utilization. 